Interface Byte Code:
            1: after compilation java compiler generates byte code in the form of dot class file for the Interface also
2: name of the generated byte code file is same as the name of the Interface
3: after compilation of nested interface java compiler uses $ sign in the name of inner interface
   byte code.

(method having  body is known have a concrete method and the class that ccontains concrete method is known as concrete class}